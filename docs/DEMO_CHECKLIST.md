# Демо и сабмит

## Сценарий на 2 минуты

Три запроса из `CLAUDE.md`, требование 17 ТЗ: плотная категория, редкая, пустой результат.
По пути закрываем вживую ещё два приёмочных теста жюри: «скрыть имена» (R14) и «две даты» (R16).
Запросы сверены с интерфейсом, прогон по таймеру — в конце раздела.

| # | Время | Что на экране | Что говорим |
|---|---|---|---|
| 1 | 0:00–0:15 | пустая форма, кнопка неактивна и пишет, чего не хватает | «В каталоге 15 ведущих, цены — от 500 тысяч до 2 миллионов, вчетверо. Непонятно, за что. Мы даём три карточки и объясняем каждую фактами» |
| 2 | 0:15–0:40 | **Ведущий, Алматы, 15.10.2026, свадьба, 900 000** → «Полная выдача», 3 карточки | «Отбор и порядок — детерминированный код, модель только пишет объяснения. У каждой карточки цена относительно бюджета, форматы, языки и цитата из описания. Плашка “Цена ориентировочная” — цена проставлена при подготовке данных, мы это не прячем» |
| 3 | 0:40–0:50 | кнопка **«Скрыть имена»** | «Без имён карточки всё равно различимы — объяснения не взаимозаменяемы» |
| 4 | 0:50–1:05 | **«А если другая дата?»** → 26.12.2026 → «Сравнить выдачу» | «Декабрь — сезон: вместо трёх свободен один. Видно, кто есть только на одной дате» |
| 5 | 1:05–1:20 | **Инструменталист, Алматы, 15.10.2026, свадьба, 1 000 000** → 3 из 3 | «Редкая категория: в городе всего трое, и все подходят. Плашка “Синтетический профиль” — такие записи были в датасете организатора, своих мы не добавляли» |
| 6 | 1:20–1:40 | **Отель, Алматы, 26.12.2026, свадьба, 3 000 000** → «Никто не подошёл», «Почему», «Что можно сделать» | «Пустой экран — не “сервис сломан”. Двое отсеяны: оба дороже бюджета и оба заняты. Одним изменением запроса не помочь, и мы так и говорим. И это сезон: в декабре свободно в среднем полотеля из двух, в ноябре — почти полтора» |
| 7 | 1:40–1:50 | журнал шагов под результатом | «Внутри: агент сам вызвал поиск, код отфильтровал и отранжировал, модель написала объяснения. Работает и без ключа — сейчас вы видите именно этот режим» |
| 8 | 1:50–2:00 | — | «Дальше: смысл описания через эмбеддинги и подбор комплекта — ведущий, фотограф и зал одного уровня под общий бюджет» |

Запасной запрос на вопрос «а если категории нет?»: **Лайв-бэнд, Астана, 20.11.2026, корпоратив,
1 500 000** → «Нет в каталоге» и подсказка, что в Алматы их пять.

Не берём на демо лайв-бэнды и национальные ансамбли в Алматы: там есть почти одинаковые
профили, и их карточки со скрытыми именами не различить (`docs/DATASET_NOTES.md`, п. 1).

**Прогон по таймеру.** Сценарий пройден в интерфейсе дважды подряд, числа — ниже. Это время
самого продукта: нажатия и ответы. Речь поверх него репетирует команда — пункты 1, 6 и 8
прочитать вслух с секундомером.

| Прогон | Запросы | Ответ на запрос | «Сравнить выдачу» | Все проверки сценария |
|---|---|---|---|---|
| 1 | 4 из 4 дали обещанный исход | 1,07–1,08 с | 1,08 с | да: подсказка у пустой формы, 3 карточки с цитатами и «Цена ориентировочная», имена скрываются, «Только на этой дате», «Синтетический профиль», «Почему» и сезон у отелей |
| 2, со свежей загрузки страницы | 4 из 4 | 1,03–1,08 с | 1,08 с | да, то же самое |

Режим — без ключа (mock): ответ приходит со следующим опросом интерфейса, раз в секунду.
С настоящей моделью ответ дольше, до 5,2 с по `docs/EVAL.md`, — на сценарий это добавит
около 15 секунд, поэтому паузы в пунктах 2–6 держим короткими.

## Перед сабмитом

Сверяется с разделом «Блокирует допуск» в `TASKS.md` — там те же пункты с ответственными.

Первые четыре пункта — условия допуска, а не пожелания.

Предварительная сверка — 16:35, по коммиту `bfa839d`. Финальная — в 17:40, после заморозки кода:
пройти заново все пункты без отметки и перепроверить `verify.py` на итоговой версии.

- [x] Проект запускается с **чистого `git clone`** строго по README, на ноуте, где его не писали (п. 5.4.15–16)
      — клон в пустую папку, свежие venv и `node_modules`: локальный вариант 16:33 (`bfa839d`), Docker — 16:16 (`07c1cc4`). Ноут без истории разработки не пробовали
- [x] `docker compose up --build` работает **без `.env`** и без ключей (п. 5.6.6) — 16:16, `07c1cc4`
- [x] `python scripts/verify.py` печатает `OK` — 16:33 на чистом клоне `bfa839d`, в том числе с `DJANGO_DEBUG=0`
- [x] README заполнен по всем разделам, включая «Использованные сторонние компоненты»
      и «Что было готово до начала соревновательной части» (п. 5.4.4, 5.4.4.2) — плейсхолдеров нет,
      раскрыты датасет организатора и модель по API
- [x] `python scripts/dev.py check` зелёный — 64 теста и `tsc --noEmit`
- [ ] `docs/HOURLY.md` заполнен за все пять часов, теги `h1`–`h5` запушены (п. 5.4.8)
      — часы 1–4 и теги `h1`–`h4` на сервере; **час 5 — после заморозки, до 17:50**
- [x] У всех троих есть коммиты под своим именем (п. 4.6) — Damir Doskulov, Nurbek Gabdullin, amangeldiyev
- [ ] Видео-демо записано и лежит по ссылке — страховка от упавшего деплоя
      — записано в 16:44: 100 с, в кадре только приложение, сценарий выше целиком — три запроса,
      «Скрыть имена», две даты рядом, журнал; режим без ключа. Снято до `55336ad`: в нём нет строки
      занятости и новых объяснений без ключа — **переснять после заморозки** и выложить по ссылке
- [ ] ~~Развёрнуто по публичной ссылке, ссылка открывается с телефона, QR готов для слайда~~
      — снято (C10): без аккаунта хостинга это риск за час до заморозки; демо — с ноутбука
- [ ] ~~На публичном деплое `DJANGO_DEBUG=0` — иначе трейсбеки Django покажут настройки всем~~
      — деплоя нет. Проверено локально: с `DJANGO_DEBUG=0` страница ошибки — 179 байт без маршрутов
      и настроек (с `DJANGO_DEBUG=1` — 3,6 КБ с ними), сценарий проходит
- [ ] ~~На публичном деплое `NEXT_PUBLIC_API_URL` указывает на реальный адрес бэкенда
      (значение вшивается при сборке образа — после смены нужен пересбор фронтенда)~~ — деплоя нет
- [x] Ключи не в репозитории; если нужен настоящий ключ — подготовлен отдельный тестовый доступ
      — скан файлов и всей истории на ключи OpenAI, NVIDIA, GitHub, AWS и приватные ключи пуст; `.env` в `.gitignore`.
      Настоящий ключ для проверки не нужен: основной сценарий идёт без него
- [x] Примеры данных реалистичны, казахстанский контекст — плюс — Алматы и Астана, той, казахский язык, цены в тенге
- [ ] Всё запушено **до 18:00**: то, что зафиксировано на этот момент, и есть итоговая версия (п. 5.4.13)
      — проверить в 17:55: `git status` чистый у всех троих, `git log origin/main` содержит последнее
